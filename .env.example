# =============================================================================
# CMU Insta - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DATABASE_URL=postgres://postgres:password@localhost:5432/cmuinsta?sslmode=disable

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Backend server port
BACKEND_PORT=8080

# Frontend dev server port
FRONTEND_PORT=5173

# -----------------------------------------------------------------------------
# Authentication (Keycloak OIDC via ScottyLabs SSO)
# -----------------------------------------------------------------------------
# ScottyLabs IDP URL
OIDC_ISSUER_URL=https://idp.scottylabs.org/realms/scottylabs

# OIDC Client credentials (keep secret - backend only!)
OIDC_CLIENT_ID=dev-proxy
OIDC_CLIENT_SECRET=your-client-secret-here

# OAuth redirect URI (where Keycloak sends users after login)
# For local development:
OIDC_REDIRECT_URI=http://localhost:5173/oauth2/callback

# For production, use your actual domain:
# OIDC_REDIRECT_URI=https://yourdomain.com/oauth2/callback

# -----------------------------------------------------------------------------
# Admin Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of Andrew IDs that have admin access
# Users with these Andrew IDs will be redirected to the admin console
ADMIN_IDS=admin1,admin2,admin3

# -----------------------------------------------------------------------------
# Storage Configuration
# -----------------------------------------------------------------------------
# Directory where uploaded posts are stored (set by justfile, usually don't need to change)
POSTS_STORE_DIR=.posts_store
